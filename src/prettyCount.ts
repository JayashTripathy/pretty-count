/**
 * 
 *
 * @description 
 *
 * @param {Number} num 
 * @param {Object} options
 */
export function PrettyCount(num: number, options: any) {
  options = options || {};
  return "Your pretified number is: " + num;
}
